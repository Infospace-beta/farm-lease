/**
 * Land state slice.
 * Extend with Zustand, Redux Toolkit, or Jotai as needed.
 */

// Example Zustand slice structure:
//
// import { create } from 'zustand';
//
// export const useLandStore = create((set) => ({
//   lands: [],
//   selectedLand: null,
//   setLands: (lands) => set({ lands }),
//   setSelectedLand: (land) => set({ selectedLand: land }),
// }));

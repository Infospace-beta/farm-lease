/**
 * Auth state slice.
 * Extend with Zustand, Redux Toolkit, or Jotai as needed.
 *
 * Currently the app uses React Context (src/providers/AuthProvider.jsx).
 * This file is a placeholder for migrating to a dedicated state manager.
 */

// Example Zustand slice structure (install zustand first):
//
// import { create } from 'zustand';
//
// export const useAuthStore = create((set) => ({
//   user: null,
//   setUser: (user) => set({ user }),
//   clearUser: () => set({ user: null }),
// }));
